1. 배열을 함수 함수 인자로 넘길 때는 size까지 넘기는 것이 좋다.
2. 배열의 이름은 배열의 첫번째 요소의 주소를 의미한다 -> 포인터 변수에 담을 수 있다
3. 배열에 인덱스를 함께 쓰면 (arr[2]) 그 인덱스가 가리키는 배열의 값을 의미한다.
4. 배열과 포인터의 가장 큰 차이점은 배열이름은 상수값이지만 포인터는 상수일수도 변수 일수도 있다는 것이다
   따라서, 포인터 = 배열이름은 가능하지만 배열이름 = 포인터는 불가능하다
